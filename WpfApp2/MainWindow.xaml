<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        
        Title="MainWindow" Height="450" Width="800"
        >
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect></DropShadowEffect>
                </Setter.Value>
            </Setter>

        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition>
</ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="Lista" DisplayMemberPath="NombreCompleto"
                 Background="Aqua"></ListBox>

        <StackPanel Grid.Column="1" Background="LightBlue" Margin="10">
            <TextBlock Text="{Binding ElementName=Lista,Path=SelectedItem.Nombre}"/>
            <TextBlock Text="{Binding ElementName=Lista,Path=SelectedItem.Apellidos}"/>
            <TextBlock Text="{Binding ElementName=Lista,Path=SelectedItem.Telefono}"/>

        </StackPanel>
    </Grid>
</Window>
